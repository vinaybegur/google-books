-unless @books.empty?
  .page-header.searched-results
    %h1
      Found results for
      %span "#{@key.input_value}"

  %table.table.table-striped
    %tr
      %th Book
      %th Title
      %th Authors
    - @books.each do |b|
      %tr
        %td= b.image_link ? image_tag(b.image_link, :size => '52x80') : "Image Not Found"
        %td= b.title
        %td= b.authors
  .paginate_books
    = link_to "Back", root_path,:class => 'btn btn-primary'
    = paginate @books
    
    :css
      #display_form{
          margin-top: 0%
      }